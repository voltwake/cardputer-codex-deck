# Current repository standardization Goal acceptance record

> **Status: implementation not started**

The active acceptance criteria are defined in [`GOAL.md`](GOAL.md). The
implementation must update this file with structure, command, regression,
build, artifact, and repository-rename evidence as each applicable gate is
completed.

Baseline before directory migration:

- local baseline commit: `ba6c95f`;
- Python tests: 82 passing;
- Swift tests: 5 passing;
- M5 firmware: build succeeds;
- full `./scripts/test.sh`: passing;
- four user-owned PNG files: untracked and excluded;
- remote repository: still `voltwake/cardputer-codex-deck`;
- installed App/Agent and M5 firmware: unchanged by this Goal preparation.

The completed multi-device Goal evidence is preserved separately in
[`MULTI_DEVICE_GOAL_ACCEPTANCE.md`](MULTI_DEVICE_GOAL_ACCEPTANCE.md).
